# Telegram Bot — /start и главная клавиатура

> **Важно:** приветствие, кнопки «О магазине», «Доставка», «Режим работы», «Профиль» и «Помощь» — это **сообщения в чате с ботом**, а не экраны Mini App.
>
> Mini App открывается **только** по кнопке «🛍️ Открыть магазин» (и «⚙️ Админ-панель» для admin).

## Два отдельных процесса

| Компонент | Что делает | Как запустить |
|-----------|------------|---------------|
| **Telegram Bot** (`bot/`) | `/start`, регистрация в `clients`, Reply Keyboard, текстовые ответы | `npm run bot` |
| **Mini App** (`src/`) | Каталог, корзина, оформление заказа | `npm run dev` / деплой |

Бот и Mini App — **разные программы**. Без запущенного бота (`npm run bot`) команда `/start` в Telegram **не сработает**, даже если Mini App уже задеплоен.

## Логика `/start`

1. Пользователь отправляет `/start`.
2. Бот читает `id`, `username`, `first_name` из `message.from`.
3. Ищет запись в таблице `clients` по `telegram_id`.
4. Если записи нет — создаёт пользователя с ролью `["client"]`.
5. Если есть — обновляет username / first_name и загружает роли.
6. Отправляет приветствие и **Reply Keyboard** (как на макете).

## Клавиатура

| Кнопка | Действие |
|--------|----------|
| 🛍️ Открыть магазин | Web App → открывает Mini App (магазин) |
| ℹ️ О магазине | **Сообщение в чате** |
| 📦 Условия доставки | **Сообщение в чате** |
| 🕐 Режим работы | **Сообщение в чате** |
| ⚙️ Админ-панель | Только для `admin` → Mini App `/#/admin` (заказы, товары, клиенты, аналитика) |
| 👤 Профиль / ❓ Помощь | **Сообщение в чате** |
| 🛟 Техподдержка | Диалог с оператором; админу — уведомление с кнопками ответа |
| ✅ Доступные заказы | Список заказов со статусом `pending` (как на макете) |
| 📋 Мои заказы | Принятые вами заказы + смена статуса |

## Уведомления и статусы (как в ТЗ)

После оплаты в Mini App:

1. Заказ сохраняется со статусом `pending`.
2. Всем админам приходит сообщение **🆕 Новый заказ #N** с адресом, суммой, типом, товарами и кнопками **Принять** / **Детали**.
3. **✅ Доступные заказы** — те же карточки для всех `pending`.

**Принять заказ** → статус `accepted`, заказ попадает в **📋 Мои заказы**, клиенту: «Заказ #N принят».

В **📋 Мои заказы** — те же карточки (адрес, сумма, тип, блюда) + строка статуса и кнопки:


| Кнопка | Статус | Уведомление клиенту |
|--------|--------|---------------------|
| 📦 Начать сборку | `preparing` | Сборка начата |
| 🚚 В доставку | `shipping` | (только курьер/почта) |
| ✅ Доставлен | `delivered` | Заказ выполнен |

Для **самовывоза** шаг «В доставку» пропускается.

**Нужно:** `npm run bot` и `ADMIN_TELEGRAM_IDS`.

**Тест без Mini App:** команда `/demo` (только admin) или `npm run bot:seed` — два заказа как на макете, затем **✅ Доступные заказы**.

## Техподдержка

Клиент: **🛟 Техподдержка** → пишет сообщение → админам приходит карточка:

- 🛟 Техническая поддержка: новое сообщение  
- 💬 Диалог #N · 👤 имя (@username) · 🆔 ID  
- **✍️ Ответить** · **🔒 Закрыть диалог** · **📜 История диалога**

Ответ админа уходит клиенту в чат. `/cancel` — отменить ввод ответа.

**Тест:** `/demo_support` (admin) — демо-сообщение «Ошибка» от клиента transcendens (@cedric_smith).

## Настройка

```bash
cd erd-tma
cp .env.example .env
# Заполни BOT_TOKEN и WEBAPP_URL
npm install
npm run bot
```

### Переменные `.env`

```env
BOT_TOKEN=123456:ABC...          # @BotFather
WEBAPP_URL=https://...           # HTTPS URL Mini App
ADMIN_TELEGRAM_IDS=123456789     # ваш Telegram ID для роли admin
```

Telegram ID можно узнать у [@userinfobot](https://t.me/userinfobot).

## База данных (Supabase / PostgreSQL)

Данные хранятся в **Supabase** — единая БД для бота, админ-панели и каталога.

### Настройка

1. Создайте проект на [supabase.com](https://supabase.com).
2. В **SQL Editor** выполните миграцию: `supabase/migrations/001_initial_schema.sql`.
3. Скопируйте ключи из **Project Settings → API** в `.env`:
   - `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` — для бота (`npm run bot`)
   - `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` — для Mini App
4. Заполните каталог и промокоды:

```bash
npm run bot:seed-catalog
```

### Таблицы

| Таблица | Назначение |
|---------|------------|
| `clients` | Пользователи Telegram, роли `["client"]` / `["client","admin"]` |
| `orders` | Заказы из Mini App + статусы для бота и админки |
| `products`, `categories` | Каталог магазина |
| `promos`, `promo_usage` | Промокоды и счётчики |
| `support_dialogues`, `support_messages` | Техподдержка |

## Запуск вместе с фронтом

```bash
# терминал 1
npm run dev

# терминал 2 (после деплоя или туннеля — WEBAPP_URL в .env)
npm run bot:dev
```

В BotFather укажи тот же `WEBAPP_URL` в **Configure Mini App**.
